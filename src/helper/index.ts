import { canMove, canMoveDown, canMoveLeft, canMoveRight, moveDown, moveLeft, moveRight, moveUp, setupInput } from './input'

export { setupInput, moveDown, moveLeft, moveRight, moveUp, canMove, canMoveRight, canMoveLeft, canMoveDown }